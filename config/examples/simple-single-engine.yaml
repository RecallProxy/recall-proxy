# Simple single-engine configuration
#
# This example shows the minimal viable setup: one semantic provider
# and a default bind address.

bind_address: "127.0.0.1:8080"

providers:
  - name: semantic_local
    kind: semantic
