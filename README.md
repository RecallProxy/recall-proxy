# recall-proxy
RecallProxy is the universal gateway between LLMs and cognitive memory. Standardize your context stack with a unified API to swap, route, and orchestrate semantic, temporal, and structural memory engines (Mem0, Cognee, pgvector) without rewriting agent logic. The neutral middleware layer for persistent AI state.
