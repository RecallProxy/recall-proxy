[package]
name = "recall-proxy-gateway"
edition = "2021"
version = "0.1.0"
license = "MIT"

[dependencies]
recall-proxy-core = { path = "../core" }
recall-proxy-config = { path = "../config" }

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros"] }
