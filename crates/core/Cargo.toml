[package]
name = "recall-proxy-core"
edition = "2021"
version = "0.1.0"
license = "MIT"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
