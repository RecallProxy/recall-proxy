[package]
name = "recall-proxy-config"
edition = "2021"
version = "0.1.0"
license = "MIT"

[dependencies]
recall-proxy-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"